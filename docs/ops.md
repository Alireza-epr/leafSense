# Operations & Security Notes

## Environment Variables

- This project does **not use any API keys or secrets**.  
- No `.env` or `.env.local` files are required.  
- All configuration is handled in the code or via props; nothing sensitive is stored in the repo.
